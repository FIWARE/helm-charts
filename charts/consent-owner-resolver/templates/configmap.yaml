apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "resolver.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "resolver.labels" . | nindent 4 }}
data:
  config.json: |
    {{- include "resolver.config" . | nindent 4 }}
