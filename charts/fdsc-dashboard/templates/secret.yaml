{{- include "fiwareCommon.secret.tpl" (dict
      "context"        $
      "existingSecret" .Values.auth.existingSecret
      "data"           (dict .Values.auth.secretKey (.Values.auth.config | toJson))) }}
