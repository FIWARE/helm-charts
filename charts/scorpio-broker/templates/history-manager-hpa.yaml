{{- if .Values.historyManager.enabled }}
{{- include "common.hpa.tpl" (dict
      "context"     $
      "component"   .Values.historyManager.name
      "autoscaling" .Values.historyManager.autoscaling) }}
{{- end }}
