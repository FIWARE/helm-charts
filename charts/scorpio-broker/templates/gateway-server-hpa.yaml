{{- if .Values.gateway.enabled }}
{{- include "fiwareCommon.hpa.tpl" (dict
      "context"     $
      "component"   .Values.gateway.name
      "autoscaling" .Values.gateway.autoscaling) }}
{{- end }}
