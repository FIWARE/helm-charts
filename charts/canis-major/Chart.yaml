apiVersion: v2
name: canis-major
version: 0.2.0
appVersion: 1.5.15
kubeVersion: '>= 1.19-0'
description: A Helm chart for running canis major on kubernetes.
icon: https://fiware.github.io/catalogue/img/fiware.png
keywords:
  - fiware
  - canismajor
  - blockchain
  - dlt
sources:
  - https://github.com/FIWARE/CanisMajor
maintainers:
  - name: wistefan
    email: stefan.wiedemann@seamware.com
# The `common` library chart ships every shared helper
# (names, labels, service-account resolution, existing-secret
# resolution, service/ingress/route/HPA/secret/serviceaccount
# bodies). It is consumed via a local `file://` dependency so
# publishing workflows do not need a separate repository entry —
# Helm packages the library alongside canis-major in the resulting
# `canis-major-<version>.tgz`. See charts/common/README.md.
dependencies:
  - name: common
    repository: "https://fiware.github.io/helm-charts"
    version: "0.0.1"
