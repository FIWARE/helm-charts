{{- if .Values.siop.enabled}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bizEcosystemLogicProxy.fullname" . }}-ccs
  labels:
    {{- include "bizEcosystemLogicProxy.labels" . | nindent 4 }}
data:
  init.sh: |-
    curl -X 'POST' \
      '{{ .Values.siop.ccs.endpoint }}/service' \
      -H 'Accept: */*' \
      -H 'Content-Type: application/json' \
      -d '{
      "id": "{{ .Values.siop.clientId }}",
      "default_scope": "{{ .Values.siop.ccs.defaultScope }}",
      {{- if .Values.siop.ccs.scopes -}}
      "scopes": {{- toJson .Values.siop.ccs.scopes }}
      {{- end }}
    }'
{{- end }}
