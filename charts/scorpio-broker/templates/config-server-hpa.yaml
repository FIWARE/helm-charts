{{- if .Values.configServer.enabled }}
{{- include "common.hpa.tpl" (dict
      "context"     $
      "component"   .Values.configServer.name
      "autoscaling" .Values.configServer.autoscaling) }}
{{- end }}
