{{- if .Values.ingress.enabled -}}
{{- $servicePort := .Values.service.port -}}
{{- $allInOneEnabled := .Values.allInOne.enabled -}}
{{- $serverHost := $.Values.defaultConfig.serverHost -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tmforum.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{ include "tmforum.labels" . | nindent 4 }}
  {{- if .Values.ingress.annotations }}
  annotations:
    {{- with .Values.ingress.annotations }}
      {{- toYaml . | nindent 4 }}
      {{- end }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className | quote }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
      {{- range .hosts }}
      - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
  {{- range .Values.ingress.hosts }}
  - host: {{ .host | quote }}
    http:
      paths:
      {{- if $allInOneEnabled }}
      {{- range .paths }}
      - path: {{ . }}
        pathType: Prefix
        backend:
          service:
            {{- if contains "tm-forum-api-svc" $serverHost }}
            name: tm-forum-api-svc
            {{- else }}
            name: {{ include "tmforum.fullname" $ }}-svc
            {{- end }}
            port:
              number: {{ $servicePort }}
      {{- end }}
      {{- else }}
      {{- range $.Values.apis }}
      - path: {{ .basePath }}
        pathType: Prefix
        backend:
          service:
            name: {{ include "tmforum.fullname" $ }}-{{ .name }}
            port:
              number: {{ $servicePort }}
      {{- end }}
      {{- end }}
  {{- end }}
{{- end }}
