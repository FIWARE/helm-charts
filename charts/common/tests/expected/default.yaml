---
# Source: common-tests/templates/resources.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::000000000000:role/common-tests
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
---
# Source: common-tests/templates/resources.yaml
apiVersion: v1
kind: Secret
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
type: Opaque
data:
  dbPassword: c2VjcmV0LXBhc3N3b3Jk
---
# Source: common-tests/templates/parity.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: test-release-common-tests-parity
  namespace: "test-ns"
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
data:
  # ---- common.names.name ---------------------------------------------
  common_names_name: "common-tests"
  legacy_names_name: "common-tests"

  # ---- common.names.fullname -----------------------------------------
  common_names_fullname: "test-release-common-tests"
  legacy_names_fullname: "test-release-common-tests"

  # ---- common.names.chart --------------------------------------------
  common_names_chart: "common-tests-0.0.0"
  legacy_names_chart: "common-tests-0.0.0"

  # ---- common.names.namespace ----------------------------------------
  common_names_namespace: "test-ns"

  # ---- common.labels.standard ----------------------------------------
  common_labels_standard: |
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
  legacy_labels: |
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm

  # ---- common.labels.matchLabels -------------------------------------
  common_labels_matchLabels: |
    app.kubernetes.io/name: common-tests
    app.kubernetes.io/instance: test-release
  legacy_matchLabels: |
    app.kubernetes.io/name: common-tests
    app.kubernetes.io/instance: test-release

  # ---- component (dict) form -----------------------------------------
  # Additive: when component is empty the dict form equals the plain
  # form; when non-empty it adds `-<component>` to the name and
  # `app.kubernetes.io/component: <component>` to the labels. Only the
  # common.* side is exercised here (legacy charts don't use this
  # variant yet).
  common_names_name_with_component: "common-tests-worker"
  common_names_fullname_with_component: "test-release-common-tests-worker"
  common_labels_standard_with_component: |
    app.kubernetes.io/name: common-tests-worker
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/component: worker
  common_labels_matchLabels_with_component: |
    app.kubernetes.io/name: common-tests-worker
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/component: worker

  # ---- component (dict) form with empty component --------------------
  # Verifies that dict-form with empty component is byte-identical to
  # plain form.
  common_names_name_dict_empty: "common-tests"
  common_names_fullname_dict_empty: "test-release-common-tests"
  common_labels_standard_dict_empty: |
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm

  # ---- common.serviceAccount.name ------------------------------------
  # Default branch: `serviceAccount.create: true`, `serviceAccount.name: ""`
  # → fullname; same as the legacy body.
  common_sa_name_default: "test-release-common-tests"
  legacy_sa_name_default: "test-release-common-tests"

  # Component variant: `create: true`, no override, component="worker"
  # → fullname-worker. Additive: legacy has no equivalent to compare
  # against, so this line is only on the common.* side.
  common_sa_name_component: "test-release-common-tests-worker"

  # ---- common.secrets.name (keyrock-style, string override) ----------
  common_secret_keyrock: "my-keyrock-secret"
  legacy_secret_keyrock: "my-keyrock-secret"

  # ---- common.secrets.name (keyrock cert, string override + suffix) --
  common_secret_keyrock_cert: "my-keyrock-cert-secret"
  legacy_secret_keyrock_cert: "my-keyrock-cert-secret"

  # ---- common.secrets.name (orion-style, map with name) --------------
  common_secret_orion: "my-orion-db-secret"
  legacy_secret_orion: "my-orion-db-secret"

  # ---- common.secrets.name (orion-style, map without name) -----------
  # Falls back to fullname because the map lacks a `name` key.
  common_secret_orion_noname: "test-release-common-tests"

  # ---- common.secrets.name (no existingSecret) -----------------------
  # Both nil (absent) and empty fall back to fullname.
  common_secret_orion_absent: "test-release-common-tests"

  # ---- common.secrets.key (orion-style, key set) ---------------------
  common_secret_key_orion: "myDbPasswordKey"
  legacy_secret_key_orion: "myDbPasswordKey"

  # ---- common.secrets.key (map without key → default) ----------------
  common_secret_key_default: "dbPassword"

  # ---- common.images.image -------------------------------------------
  # `image.tag: ""` with .Chart.AppVersion="1.0.1" → repository:1.0.1;
  # matches the `tag | default .Chart.AppVersion` idiom used in
  # onboarding-portal / did-helper / odrl-pap / etc.
  common_image_appversion: "quay.io/fiware/common-tests:1.0.1"
  legacy_image_appversion: "quay.io/fiware/common-tests:1.0.1"

  # Explicit tag — verifies the helper does not override a caller-set
  # value (orion / keyrock / mintaka path).
  common_image_pinned: "quay.io/fiware/common-tests:2.0.0"
  legacy_image_pinned: "quay.io/fiware/common-tests:2.0.0"

  # ---- common.images.pullPolicy --------------------------------------
  # Empty → IfNotPresent (Kubernetes default).
  common_pullPolicy_default: "IfNotPresent"

  # Explicit → passthrough.
  common_pullPolicy_pinned: "Always"

  # ---- common.images.pullSecrets -------------------------------------
  # Mix of `- name: ...` and bare strings in the input; both normalise
  # to canonical `- name: ...` form.
  common_pullSecrets: |
    imagePullSecrets:
      - name: registry-a
      - name: registry-b

  # Empty input renders nothing (empty string), so the consumer's
  # deployment template can `include` this unconditionally.
  common_pullSecrets_empty: ""
---
# Source: common-tests/templates/resources.yaml
apiVersion: v1
kind: Service
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  annotations:
    service.beta.kubernetes.io/topology-aware-hints: auto
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 1026
      targetPort: 1026
      protocol: TCP
      name: "http"
  selector:
    app.kubernetes.io/name: common-tests
    app.kubernetes.io/instance: test-release
---
# Source: common-tests/templates/resources.yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: test-release-common-tests
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - resource:
        name: cpu
        target:
          averageUtilization: 80
          type: Utilization
      type: Resource
---
# Source: common-tests/templates/resources.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - "common.example.org"
      secretName: common-tls
  rules:
    - host: "common.example.org"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: test-release-common-tests
              port:
                number: 1026
---
# Source: common-tests/templates/resources.yaml
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: test-release-common-tests
  namespace: "test-ns"
  labels:
    app.kubernetes.io/name: common-tests
    helm.sh/chart: common-tests-0.0.0
    app.kubernetes.io/instance: test-release
    app.kubernetes.io/version: "1.0.1"
    app.kubernetes.io/managed-by: Helm
  annotations:
    haproxy.router.openshift.io/timeout: 30s
spec:
  host: common.apps.example.org
  to:
    kind: Service
    name: test-release-common-tests
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
