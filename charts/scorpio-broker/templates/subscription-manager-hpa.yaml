{{- if .Values.subscriptionManager.enabled }}
{{- include "common.hpa.tpl" (dict
      "context"     $
      "component"   .Values.subscriptionManager.name
      "autoscaling" .Values.subscriptionManager.autoscaling) }}
{{- end }}
