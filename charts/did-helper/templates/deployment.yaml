apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "did-helper.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "did-helper.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "did-helper.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "did-helper.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.config.server.serverPort }}
              protocol: TCP
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- with .Values.volumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.config.provideKeystore.enabled }}
            - name: keystore-volume
              mountPath: "/etc/keystore/keystore.pfx"
              subPath: "keystore.pfx"
              readOnly: true
            {{- end }}
          env:
            {{- if and .Values.config.server.keystorePasswordSecretName .Values.config.server.keystorePasswordSecretKey }}
            - name: STORE_PASS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.config.server.keystorePasswordSecretName }}
                  key: {{ .Values.config.server.keystorePasswordSecretKey }}
            {{- end }}
            {{- range $key, $value := omit .Values.config.server "keystorePasswordSecretName" "keystorePasswordSecretKey"}}
            - name: {{ $key | snakecase | upper }}
              valueFrom:
                configMapKeyRef:
                  name: {{ include "did-helper.fullname" $ }}-cm
                  key: {{ $key }}
            {{- end }}
            {{- if .Values.config.generateKey.enabled }}
            {{- range $key, $value := omit .Values.config.generateKey "enabled" }}
            - name: {{ $key | snakecase | upper }}
              valueFrom:
                configMapKeyRef:
                  name: {{ include "did-helper.fullname" $ }}-cm
                  key: {{ $key }}
            {{- end }}
            {{- end }}
            {{- if .Values.config.provideKeystore.enabled }}
            {{- range $key, $value := omit .Values.config.provideKeystore "enabled" "keystoreSecretName" "keystoreSecretKey" }}
            - name: {{ $key | snakecase | upper }}
              valueFrom:
                configMapKeyRef:
                  name: {{ include "did-helper.fullname" $ }}-cm
                  key: {{ $key }}
              {{- end }}
            - name: KEYSTORE_PATH
              value: "/etc/keystore/keystore.pfx"
            {{- end }}
      volumes:
        {{- with .Values.volumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.config.provideKeystore.enabled }}
        - name: keystore-volume
          secret:
            secretName: {{ .Values.config.provideKeystore.keystoreSecretName }}
            items:
              - key: {{ .Values.config.provideKeystore.keystoreSecretKey }}
                path: keystore.pfx
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
