apiVersion: v2
name: common
version: 0.1.0
type: library
kubeVersion: '>= 1.19-0'
description: >-
  FIWARE common library chart. Bundles the template helpers (names, labels, service account, secret, image, and resource bodies) that are currently duplicated across every FIWARE Helm chart. Consumer charts depend on it locally and include its `fiwareCommon.*` templates instead of re-implementing the same boilerplate per chart.
home: https://github.com/FIWARE/helm-charts/tree/main/charts/common
icon: https://fiware.github.io/catalogue/img/fiware.png
keywords:
  - fiware
  - common
  - library
sources:
  - https://github.com/FIWARE/helm-charts
maintainers:
  - name: wistefan
    email: stefan.wiedemann@seamware.com
